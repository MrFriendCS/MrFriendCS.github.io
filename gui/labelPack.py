# Get extra code
from tkinter import *

# Create an interface
root = Tk()

# Add a title
root.title("Label - Pack")

# Create a label
myLabel = Label(root, text="Hello World!")

# Add the label to the interface
myLabel.pack()

# Display the GUI
root.mainloop()
